//REQUIRED CLASS
package project4;

import project1.*;
import datalogProgram.*;
//import project2.*;

import java.util.HashSet;

public class TupleSet {
/*  HashSet<Tuple> tupleSet

  // Constructor
  TupleSet() {
    // TODO: create an empty set of tuples
  }
  
  public Iterator<Tuple> iterator() {
    return tupleSet.iterator();
  }

  public void add(Tuple tuple) {
    if (!tupleSet.add(tuple)) {
      System.out.println("Warning: tuple " + tuple +
                         " not added to set.");
    }
  }

  public TupleSet union(TupleSet otherSet) {
    // Union the this and otherSet
  }*/
}
